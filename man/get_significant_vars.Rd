% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jackstraw.R
\name{get_significant_vars}
\alias{get_significant_vars}
\title{Extract significant variables from jackstraw results}
\usage{
get_significant_vars(jackstraw_result, block = 1L, component = 1L)
}
\arguments{
\item{jackstraw_result}{An object of class \code{"jackstraw_rajive"}.}

\item{block}{Positive integer; which block.}

\item{component}{Positive integer; which joint component.}
}
\value{
A character vector of variable names (when the original data matrix
  had column names) or an integer vector of column indices.
}
\description{
Convenience accessor that retrieves the vector of significant variables
(either column names or integer indices) for a given block and joint
component.
}
\examples{
\donttest{
set.seed(42)
n   <- 50
pks <- c(100, 80)
Y   <- ajive.data.sim(K = 2, rankJ = 2, rankA = c(5, 4), n = n,
                      pks = pks, dist.type = 1)
data.ajive           <- Y$sim_data
initial_signal_ranks <- c(5, 4)
ajive_result <- Rajive(data.ajive, initial_signal_ranks)
js <- jackstraw_rajive(ajive_result, data.ajive, alpha = 0.05, n_null = 10)
get_significant_vars(js, block = 1, component = 1)
}

}
