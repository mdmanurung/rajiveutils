% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{assess_stability}
\alias{assess_stability}
\alias{bootstrap_joint_rank}
\alias{bootstrap_loading_stability}
\title{Assess stability of AJIVE decomposition via bootstrap resampling}
\usage{
assess_stability(
  ajive_output = NULL,
  blocks,
  initial_signal_ranks,
  target = c("joint_rank", "loadings", "components"),
  method = c("bootstrap", "permutation"),
  B = 100L,
  n_perm = 100L,
  sample_frac = 0.8,
  num_cores = 1L,
  cluster = NULL,
  strata = NULL,
  resample = NULL,
  return_replicates = FALSE,
  ...
)

bootstrap_joint_rank(
  ajive_output = NULL,
  blocks,
  initial_signal_ranks,
  B = 100L,
  sample_frac = 0.8,
  num_cores = 1L
)

bootstrap_loading_stability(
  ajive_output,
  blocks,
  initial_signal_ranks,
  B = 100L,
  sample_frac = 0.8,
  num_cores = 1L
)
}
\arguments{
\item{ajive_output}{An object of class \code{"rajive"} (output of
\code{\link{Rajive}}).  Required for \code{target = "loadings"} and
\code{target = "components"}.}

\item{blocks}{List of data matrices (same list passed to
\code{\link{Rajive}}).}

\item{initial_signal_ranks}{Integer vector of initial signal ranks for each
block.}

\item{target}{Character scalar.  One of \code{"joint_rank"},
\code{"loadings"}, or \code{"components"}.}

\item{method}{Character scalar.  Only \code{"bootstrap"} is currently
implemented.  \code{"permutation"} is reserved and errors explicitly
rather than silently returning bootstrap results.}

\item{B}{Positive integer.  Number of bootstrap replicates.  Default 100.}

\item{n_perm}{Reserved positive integer for a future permutation stability
implementation.  Currently unused because \code{method = "permutation"}
is rejected.}

\item{sample_frac}{Numeric in (0, 1].  Fraction of samples to draw in each
bootstrap replicate.  Default 0.8.}

\item{num_cores}{Positive integer.  Number of cores for parallel execution.
Default 1 (no parallelism).}

\item{cluster}{Optional cluster identifier per sample.  When supplied and
\code{resample} is \code{NULL}, bootstrap refits resample whole clusters.}

\item{strata}{Optional stratum identifier per sample for stratified
cluster bootstrap.}

\item{resample}{Character scalar or \code{NULL}.  \code{"observation"}
resamples rows; \code{"cluster"} resamples clusters.  \code{NULL}
chooses cluster resampling when \code{cluster} is supplied.}

\item{return_replicates}{Logical.  When \code{TRUE}, the returned summary
carries bootstrap arrays in \code{attr(result, "replicates")}.}

\item{...}{Reserved for future arguments.}
}
\value{
A named list whose structure depends on \code{target}:
\describe{
  \item{\code{"joint_rank"}}{A list with fields \code{rank_distribution}
    (integer vector of length \code{B}), \code{rank_table} (frequency
    table), and \code{observed_rank} (integer).}
  \item{\code{"loadings"}}{A list with one element per block.  Each element
    contains \code{mean_loading} (aligned mean loading matrix),
    \code{sd_loading} (element-wise standard deviation after alignment), and
    \code{cos_similarity} (vector of mean cosine similarities per
    component).  All summaries are computed after orthogonal Procrustes
    alignment.}
}
}
\description{
Evaluates the stability of the estimated joint rank or block loadings via
bootstrap resampling of the sample dimension.
}
\section{Procrustes alignment for loading stability}{

When \code{target = "loadings"}, bootstrap loading matrices are aligned to
the reference (full-data) loading matrix using orthogonal Procrustes
rotation before any variability summary is computed.  Raw bootstrap loading
variation is dominated by rotational indeterminacy and is not interpretable
without alignment.  All returned stability summaries are computed
\strong{after} Procrustes alignment.
}

\examples{
\donttest{
n   <- 40; pks <- c(30, 20)
Y   <- ajive.data.sim(K = 2, rankJ = 2, rankA = c(4, 3),
                      n = n, pks = pks, dist.type = 1)
fit <- Rajive(Y$sim_data, c(4, 3))
stab <- assess_stability(fit, Y$sim_data, c(4, 3),
                         target = "joint_rank", B = 50)
stab$rank_table
}

}
