% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{associate_all_components}
\alias{associate_all_components}
\title{Associate all joint and individual component scores with metadata}
\usage{
associate_all_components(
  ajive_output,
  metadata,
  variable = NULL,
  variables = NULL,
  mode = c("continuous", "categorical", "survival", "batch"),
  method = NULL,
  adjust = "BH",
  time_col = NULL,
  status_col = NULL,
  split = c("none", "median", "tertile"),
  include = c("both", "joint", "individual"),
  blocks_to_include = NULL,
  propagate_uncertainty = c("none", "bootstrap"),
  B = 200L,
  level = 0.95,
  alpha_stability = 0.05,
  blocks = NULL,
  initial_signal_ranks = NULL,
  cluster = NULL,
  strata = NULL,
  num_cores = 1L,
  ...
)
}
\arguments{
\item{ajive_output}{An object of class \code{"rajive"} (output of
\code{\link{Rajive}}).}

\item{metadata}{A \code{data.frame} of sample-level metadata; rows must
correspond in order to the samples in \code{ajive_output}.}

\item{variable}{Character scalar.  Name of a single column in
\code{metadata} to test.}

\item{variables}{Character vector.  Names of multiple columns to test.
Ignored when \code{variable} is supplied.}

\item{mode}{Character scalar.  One of \code{"continuous"},
\code{"categorical"}, \code{"survival"}, or \code{"batch"}.}

\item{method}{Character scalar.  Test method; defaults are Pearson
correlation (\code{"pearson"}) for continuous, Kruskal-Wallis
(\code{"kruskal"}) for categorical/batch, and log-rank
(\code{"logrank"}) for survival.  Categorical and batch modes currently
accept only \code{NULL} or \code{"kruskal"}; unsupported labels error.}

\item{adjust}{Character scalar.  P-value adjustment method passed to
\code{\link[stats]{p.adjust}}.  Default \code{"BH"}.}

\item{time_col}{Character scalar.  Name of the time column in
\code{metadata} (required for \code{mode = "survival"}).}

\item{status_col}{Character scalar.  Name of the event-status column in
\code{metadata} (required for \code{mode = "survival"}).}

\item{split}{Character scalar.  Score-split strategy for survival analysis.
One of \code{"none"} (continuous Cox, recommended for primary inference),
\code{"median"}, or \code{"tertile"}.}

\item{include}{Character scalar. \code{"both"} (default) tests joint and
individual scores, \code{"joint"} tests joint scores only, and
\code{"individual"} tests individual scores only.}

\item{blocks_to_include}{Optional integer vector of block indices to screen
for individual scores. \code{NULL} uses all positive-rank individual
blocks.}

\item{propagate_uncertainty}{Character scalar. \code{"none"} preserves the
historical fixed-score analysis; \code{"bootstrap"} adds score-refit
uncertainty summaries.}

\item{B}{Positive integer. Number of bootstrap refits when replicates are
not supplied.}

\item{level}{Numeric confidence level for bootstrap effect-size quantiles.}

\item{alpha_stability}{Numeric p-value threshold used to compute the
bootstrap stability fraction.}

\item{blocks}{List of original block matrices. Required for automatic
bootstrap propagation when \code{replicates = NULL}.}

\item{initial_signal_ranks}{Integer vector used for bootstrap refits when
\code{replicates = NULL}.}

\item{cluster}{Optional cluster identifier per sample for cluster bootstrap.}

\item{strata}{Optional stratum identifier per sample for stratified cluster
bootstrap.}

\item{num_cores}{Positive integer. Reserved for bootstrap execution.}

\item{...}{Reserved for future arguments.}
}
\value{
A \code{data.frame} with columns \code{source}, \code{block},
  \code{variable}, \code{component}, \code{stat}, \code{p_value},
  \code{p_adj}, and \code{method}.  With
  \code{propagate_uncertainty = "bootstrap"}, bootstrap uncertainty columns
  from \code{\link{associate_components}} are appended.  P-value adjustment
  is recomputed across the combined table.
}
\description{
Screens sample metadata associations across joint scores and block-specific
individual scores from a \code{\link{Rajive}} decomposition.  This is a
convenience wrapper around \code{\link{associate_components}} that returns a
single tidy table with explicit score-family identity.
}
\section{Bootstrap interpretation}{

Automatic bootstrap propagation aligns each score family to its own
full-data fitted basis before associations are recomputed.  Individual score
summaries are therefore basis-conditional and can be rotation-sensitive when
block-specific individual singular values are close.  Use
\code{n_boot_valid} to check how many bootstrap refits contributed to each
row.
}

